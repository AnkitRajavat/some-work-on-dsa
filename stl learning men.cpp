#include<iostream>
#include<vector>
#include<algorithm>
#include<set>
#include<list>
#include<pair>
 using namespace std;
int main(){
    pair<int ,int >p;

/*
    set<int>s;
    s.insert(5);
    s.insert(55);
    s.insert(52);
    s.insert(534);
    s.insert(34);
    s.insert(456);

    for(int x:s){
        cout<<x<<" ";
    }
    auto it =s.find(52);
    if(it == s.end()){
    cout<<" not present in set   ";

    }
    else{
        cout<<"this is present in set";

    }

    list<int>t;
    t.push_back(2);

    t.push_back(25);
     t.push_back(20);
      t.push_back(12);
       t.push_back(42);
       t.sort();
      // list<int>::iterator it;
       for(auto it=t.begin();it!=t.end();it++){
        cout<<"  "<<*it;

       }



*/










  /* vector<int>a={2,4,56,45,67,5,6};
   cout<< a[1]<<"\n";
   a.push_back(100);
   cout<<a[4]<<"\n";
   sort(a.begin() ,a.end());
   a.push_back(100);
  a.push_back(123);
   a.push_back(50);
    a.push_back(150);
     a.push_back(600);
   for(int i=0;i<=16;i++){

    cout<<a[i]<<" ";
   }
   cout<<"\n";
bool present=binary_search(a.begin(),a.end(),100);// true
sort(a.begin() ,a.end());
for(int x:a){
    cout<<x<<"  " ;

}*/
}
